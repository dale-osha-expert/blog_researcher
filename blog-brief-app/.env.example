# Required in milestone 2 — server-side Claude API calls for real brief generation.
ANTHROPIC_API_KEY=

# Optional (v1.1) — if set, ground keyword/SERP data with Semrush instead of model guesses.
SEMRUSH_API_KEY=

# Optional (v1.1) — same optional grounding pattern via NeuronWriter.
NEURONWRITER_API_KEY=
